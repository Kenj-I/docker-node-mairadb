# docker-node-mariadb

build node from ubuntu

## git clone


## init

```shell
$ sh init.sh
```

## write env

.env and build/envfile

## usage

```shell
$ docker-compose up --no-start
$ docker-compose up -d
```

```shell
$ docker-compose stop
```
